{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "id",
      "projectId",
      "name",
      "type",
      "description",
      "preCondition",
      "steps",
      "expectedResults",
      "parentId",
      "displayOrder",
      "createdAt",
      "updatedAt",
      "children"
    ],
    "properties": {
      "id": { "type": "string" },
      "projectId": { "type": "string" },
      "name": { "type": "string" },
      "type": { "type": "string", "enum": ["folder", "testcase"] },
      "description": { "type": "string" },
      "preCondition": { "type": ["string", "null"] },
      "steps": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["stepNumber", "description", "expectedResult"],
          "properties": {
            "stepNumber": { "type": "integer" },
            "description": { "type": "string" },
            "expectedResult": { "type": "string" }
          }
        }
      },
      "expectedResults": { "type": "string" },
      "parentId": { "type": ["string", "null"] },
      "displayOrder": { "type": "integer" },
      "createdAt": { "type": "string", "format": "date-time" },
      "updatedAt": { "type": "string", "format": "date-time" },
      "children": {
        "type": "array",
        "items": { "$ref": "#" }
      }
    }
  }
}
