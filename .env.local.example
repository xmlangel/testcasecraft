# bootRunRemote.sh 가 읽는 로컬 전용 설정. 이 파일을 .env.local 로 복사해 값을 채운다.
# .env.local 은 .gitignore 대상이다 — 실제 키를 저장소에 커밋하지 않는다.

# 원격 DB(remote 프로파일)의 jira_config 를 복호화하기 위한 키.
# 서버가 쓰는 것과 동일한 AES-256 Base64 키여야 한다. 커밋된 DEV 기본 키로는
# 복호화가 실패하고 Jira 이슈 검색·연결 확인이 조용히 0건이 된다.
JIRA_ENCRYPTION_KEY=
