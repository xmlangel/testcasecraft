# ===================================
# Docker Compose 운영환경 설정 샘플
# ===================================
# .env.example - Docker 운영 배포용 환경변수 템플릿
# 사용법: cp .env.example .env 후 실제 값으로 수정

# ===================================
# 서버 기본 설정
# ===================================
# 도메인 또는 IP 주소 (중요: 실제 서버 주소로 변경 필요!)
DOMAIN_NAME=your-domain.com
# 예시:
# DOMAIN_NAME=192.168.29.120
# DOMAIN_NAME=testcase.company.com
# DOMAIN_NAME=qaspecialist.shop

# HTTPS 사용 여부
ENABLE_HTTPS=false
# HTTPS 사용 시 true로 변경하고 SSL 인증서 설정 필요

# ===================================
# ICT-321/ICT-322: 원격 접속 지원 설정
# ===================================
# 프론트엔드 API 엔드포인트 (ICT-321)
REACT_APP_API_BASE_URL=http://your-domain.com:8080
# 예시:
# REACT_APP_API_BASE_URL=http://192.168.29.120:8080
# REACT_APP_API_BASE_URL=https://testcase.company.com:8080

# CORS 허용 Origin (ICT-322)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://your-domain.com:3000,http://your-domain.com:8080
# 예시:
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://192.168.29.120:3000,http://192.168.29.120:8080
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://testcase.company.com

# ===================================
# 데이터베이스 설정
# ===================================
# PostgreSQL 데이터베이스 설정
POSTGRES_DB=testcase_management
POSTGRES_USER=testcase_user
POSTGRES_PASSWORD=your_strong_postgres_password_here
# 보안: 강력한 비밀번호로 변경 필요!

# ===================================
# 보안 설정 (중요!)
# ===================================
# JWT Secret Key (512비트 이상 Base64 인코딩)
JWT_SECRET=your_512_bit_jwt_secret_key_base64_encoded_here
# 생성 방법: openssl rand -base64 64

# JIRA 암호화 키 (32바이트 Base64 인코딩)
JIRA_ENCRYPTION_KEY=5CBRv5FwesBJkQ7ecX1KGCxyUQTcnE1CkkGBYDswb2Y=
# 새로운 키 생성: openssl rand -base64 32

# ===================================
# 애플리케이션 설정
# ===================================
# 파일 업로드 경로
UPLOAD_PATH=/app/uploads

# 서버 포트 (기본값)
SERVER_PORT=8080

# ===================================
# 설정 예시 (주요 시나리오별)
# ===================================

# 🏠 로컬 네트워크 서버 (192.168.x.x)
# DOMAIN_NAME=192.168.29.120
# REACT_APP_API_BASE_URL=http://192.168.29.120:8080
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://192.168.29.120:3000,http://192.168.29.120:8080
# ENABLE_HTTPS=false

# 🌐 도메인 서버 (HTTP)
# DOMAIN_NAME=testcase.company.com
# REACT_APP_API_BASE_URL=http://testcase.company.com:8080
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://testcase.company.com
# ENABLE_HTTPS=false

# 🔒 도메인 서버 (HTTPS)
# DOMAIN_NAME=testcase.company.com
# REACT_APP_API_BASE_URL=https://testcase.company.com:8080
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://testcase.company.com
# ENABLE_HTTPS=true

# ===================================
# 보안 주의사항
# ===================================
# 1. 이 파일을 .gitignore에 추가하여 Git에 커밋하지 마세요
# 2. JWT_SECRET과 POSTGRES_PASSWORD는 반드시 강력한 값으로 변경하세요
# 3. 운영환경에서는 파일 권한을 600으로 설정하세요: chmod 600 .env
# 4. 정기적으로 비밀번호와 키를 로테이션하세요