---
name: manual-sync
description: testcasecraft 사용자 매뉴얼(`docs/manual/new/USER_MANUAL.md` + `docs/manual/*.md`) 본문을 코드·UI 변경 또는 manual-capture 의 감사 결과에 맞춰 동기화한다. 17 섹션 + 부록 + 관리자 섹션 구조를 보존하며, 누락된 라우트·기능에 신규 섹션을 삽입하고, 변경된 컴포넌트에 대한 캡션·이미지 참조를 갱신한다. 트리거 — "매뉴얼 동기화", "USER_MANUAL.md 갱신", "매뉴얼 본문 수정", "이미지 변경에 맞춰 캡션 수정", "누락 섹션 추가", "manual sync", "매뉴얼 톤 다듬기", "user_management_manual 코드와 맞추기". 캡처/감사 자체는 `manual-capture` 사용. 신규 매뉴얼 작성(0 → 1)은 본 스킬보다 사람이 직접.
---

# manual-sync

## 무엇을 하는가

기존 매뉴얼의 **부분 패치**를 안전하게 수행한다. `manual-writer-agent` 가 본 스킬의 원칙을 따라 본문을 수정한다.

대상 파일:
1. **메인**: `docs/manual/new/USER_MANUAL.md` (583줄, 17 섹션) — 한 페이지 완전판
2. **챕터별**: `docs/manual/*.md` (14개) — 항목별 분리본

같은 변경이 두 곳에 영향을 주면 둘 다 갱신한다.

## 17 섹션 구조 (불변)

```
§1. 회원가입과 로그인       — 1-1 화면 / 1-2 폼 / 1-3 로그인
§2. 프로젝트 만들기         — 2-1 다이얼로그 / 2-2 카드
§3. 화면 구성과 헤더        — 3-1 헤더 / 3-2 JIRA 패널
§4. 테스트 케이스 작성      — 4-1~4-4 화면·입력모드·행추가·트리
§5. 트리 드래그앤드롭       — 5-1~5-4 동작·다중선택·차단·로그
§6. 대시보드와 통계
§7. 테스트 플랜
§8. 테스트 실행
§9. 테스트 결과
§10. 자동화 테스트
§11. RAG 문서 & 챗
§12. 탐색 세션 (SBTM)
§13. 사용자 프로필 & 설정   — 13-1~13-7 (기본/비번/언어/JIRA/Sheets/API/테마)
§14. 헤더 도구             — 14-1~14-3 (다크모드/언어/프로젝트선택)
§15. 로그아웃
§16. 부록                  — JIRA 연동·도커 환경변수·트러블슈팅
§17. 시스템 관리자 (ADMIN)  — 관리메뉴/전체대시보드/조직/사용자/메일/LLM/스케줄러/번역/프로젝트단위
§18. 용어 정리 (glossary)
```

**번호 시프트 금지**: 이미지 파일명(`20_project_overview.png`, `78_admin_menu_dropdown.png` 등)이 섹션 번호와 느슨하게 매칭되어 있으므로, 중간에 새 섹션을 끼우면 뒤따르는 번호를 옮기지 말고 소제목 단위(`X-N`)에서 흡수한다.

## 핵심 원칙

| 원칙 | 적용 |
|------|------|
| **변경분만** | 전면 재작성·재배열 금지. 패치는 항상 부분적 |
| **코드 기준** | 매뉴얼과 코드가 다르면 코드를 진실로 봄. 컴포넌트 파일·라우트 정의(`src/main/frontend/src/App.jsx`)·data-testid 확인 |
| **백틱 일관성** | 경로(`/projects`, `/dashboard`)는 백틱으로 — manual_capture.py 감사 매처가 백틱 안 경로만 추출 |
| **이미지 참조 보존** | `![한국어 캡션](images/NN_slug.png)` 패턴 유지. 슬러그 변경 시 manual_capture.py STEPS 도 같이 변경 |
| **톤** | 한국어 존댓말, 친근하지만 간결. 명령형/지시형 (`~합니다`, `~하세요`) |
| **추측 금지** | 코드에서 확인 안 되는 기능 적지 않음 |

## 패치 패턴 — 누락 경로 → 신규 섹션

manual-capture 의 감사 리포트가 `/projects/:id/testcases` 누락이라고 보고했고, 실제 매뉴얼 §4 가 화면 흐름만 적고 URL 백틱이 빠져 있으면:

**Before:**
```markdown
### 4-1. 화면 구성

프로젝트 대시보드에서 **테스트케이스** 탭을 클릭하면...
```

**After:**
```markdown
### 4-1. 화면 구성

프로젝트 대시보드에서 **테스트케이스** 탭을 클릭하면 `/projects/{projectId}/testcases` 화면이 표시됩니다.
```

→ URL 한 줄 추가로 감사 통과, 본문 흐름 변경 0.

## 패치 패턴 — 코드 변경 반영

PR 커밋 메시지나 사용자 보고에서 "UI 변경" 단서가 있으면:

1. 변경된 컴포넌트 파일 확인 (예: `TestCaseSpreadsheet.jsx` 의 한글 30자 워드랩)
2. 매뉴얼에서 그 화면을 다루는 섹션 찾기 (§4-1 또는 §4-4)
3. 변경된 동작에 한 줄 보강:
   ```markdown
   > **2026-05-28 변경**: 셀 내용이 한글 30자(약 30em)를 넘으면 자동으로
   > 워드랩되어 다음 줄로 이어집니다. 컬럼 폭이 더 이상 가로로 늘어나지 않습니다.
   ```
4. 캡처 갱신이 필요하면 manual-capture 호출 안내

## 패치 패턴 — 라우트 제거

코드에서 라우트가 사라졌고, 매뉴얼에 백틱으로 남아 있으면:
1. 본문 검색: `grep -n "/removed-route" docs/manual/new/USER_MANUAL.md`
2. 해당 섹션 제거 또는 "기존 X 기능은 Y 로 대체됨" 메모로 변환
3. 이미지 파일은 즉시 삭제하지 말고 `docs/manual/new/images/_archive/` 로 이동 (롤백 가능성)

## 패치 패턴 — 챕터 매뉴얼 ↔ 통합 매뉴얼 동기화

`docs/manual/user_management_manual.md` 같은 챕터 매뉴얼이 별도로 존재한다. 통합본 §17 의 사용자 관리 섹션과 같은 정보를 다른 깊이로 다룬다:
- 통합본: 사용자가 처음 둘러볼 수준의 개요
- 챕터본: 운영자가 필요할 때 깊이 들여다보는 레퍼런스

한쪽이 갱신되면 다른 쪽도 같이 보강한다. 정보 중복은 허용 (각각 다른 독자), 하지만 모순은 허용 안 함.

이전 사례: `bdf71b6a docs(manual): user_management_manual.md 실제 코드와 동기화` — 챕터본만 갱신, 통합본은 그대로 → 다음 갱신 시 통합본 §17 도 같이 확인할 것.

## 종료 보고

패치 완료 후 `_workspace/manual-capture/run-{date}/manual_diff.md` 에 다음을 기록:

```markdown
# manual-sync 패치 보고

## 수정된 파일
- docs/manual/new/USER_MANUAL.md (§4-1, §17-3)
- docs/manual/user_management_manual.md (§3)

## 추가/수정/삭제
| 종류 | 섹션 | 변경 사유 |
|------|------|----------|
| 추가 | §4-1 URL 백틱 | manual-capture 감사 누락 경로 |
| 수정 | §4-4 워드랩 메모 | TestCaseSpreadsheet.jsx 변경 (2026-05-28) |

## 새로 참조하는 이미지
- (없음 / 또는 슬러그 목록)

## 미해결
- /organizations/:id — 조직 데이터 없어 본문 추가 보류, 데이터 시드 후 재시도

## 다음 액션
- manual-capture 재실행 권장 여부: NO / YES (이유)
```

## 보존해야 할 톤·표기 예시

- 한국어 한자·일본어 한자 금지, 한글 권장
- 영문 약어는 처음 등장 시 풀어쓰기: "RAG (Retrieval-Augmented Generation)" 1회 → 이후 RAG
- 코드/경로/식별자: 백틱 ``
- 강조: **굵게** (이탤릭 *는 잘 안 씀)
- 인용 박스 `> ` 는 "팁/주의/변경 메모" 같은 메타 정보에만
- 표는 가능한 한 컴팩트하게, 5컬럼 이내

## 안티패턴 (피할 것)

- ❌ 전면 재작성 — 톤 일관성·git diff 노이즈
- ❌ 섹션 번호 재배열 — 이미지 파일명 ↔ 섹션 번호 매핑 깨짐
- ❌ 코드에 없는 기능 설명 — 잘못된 정보 전파
- ❌ 백틱 없는 경로 표기 — 감사 매처에서 인식 못 함
- ❌ 영문 음역 ("아이콘", "버튼", "메뉴" 는 한국어로 적되 영어 용어 "modal", "dialog" 가 더 정확하면 백틱)
- ❌ 이미지 alt 텍스트 누락/영어
