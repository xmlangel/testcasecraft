# 관리자설정(S11) 요건반영목록

> 화면 ID **S11** 기준 문서: [`01`](01_관리자설정_업무프로세스.md) [`02`](02_관리자설정_화면정의.md) [`03`](03_관리자설정_컴포넌트.md)
> 기준 버전 **v1.0.102** 기준 커밋 `73340e4f` 작성 2026-08-03 21:42 KST
> 상태 표기·근거 규약은 [`../README.md`](../README.md) 4절.

**요건 46건**: 정상 36 숨김 2 정정 대상 4 확인 필요 4

---

## 1. 기능 요건

### 1.1 조직 관리 (S11-01~09)

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-01** | 조직을 카드로 나열하고 프로젝트·멤버 수를 표시한다 | 02 영역 B(목록) | 조직 목록 조회 | 정상 |
| **S11-02** | 조직 카드 클릭으로 상세 화면에 진입한다 | 02 영역 A·B(상세) | 상세 화면 라우팅 | 정상 |
| **S11-03** | 조직 상세에서 정보·멤버·그룹 3개 탭을 전환한다 | 02 영역 C·D | 탭 전환 동작 | 정상 |
| **S11-04** | 멤버 탭에서 사용자를 초대하고 역할을 부여한다 | 02 영역 C | 멤버 초대 메일 발송 의존 | 정상 |
| **S11-05** | 그룹 탭에서 권한 매트릭스(행: 기능, 열: 역할)를 편집한다 | 02 영역 D | 권한 매트릭스 편집 | 정상 |
| **S11-06** | 조직 수정(이름·설명·로고) | 02 영역 D(설정 탭) | 조직 정보 저장 | 정상 |
| **S11-07** | 조직 삭제 | 02 영역 D(설정 탭) | 조직 삭제 기능 | 정상 |
| **S11-08** | 조직 생성 다이얼로그 | 02 영역 A(`[조직 생성]` 버튼) | 조직 생성 | 정상 |
| **S11-09** | 상세에서 뒤로가기는 목록으로 돌아간다 | 02 영역 A(상세) | 뒤로 가기 버튼 동작 | 정상 |

### 1.2 사용자 관리 (S11-10~15)

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-10** | 사용자 목록 조회(통계 카드 4개 표시) | 02 영역 A | 사용자 목록 조회 | 정상 |
| **S11-11** | 검색(이름·이메일·사용자명) + 필터(역할/상태) | 02 영역 B | 검색·필터 실시간 적용 | 정상 |
| **S11-12** | 사용자 행마다 우측 `⋮` 메뉴 | 02 영역 D | 행 액션 메뉴 표시 | 정상 |
| **S11-13** | 비밀번호 재설정 임시 비밀번호 메일 발송 | 02 영역 D(메뉴) | 비밀번호 재설정 메일 발송 의존 | 정상 |
| **S11-14** | 역할 변경(ADMIN ↔ MANAGER ↔ null) | 02 영역 D(메뉴) | 사용자 역할 변경 | 정상 |
| **S11-15** | 활성화·비활성화 토글 | 02 영역 D(메뉴) | 사용자 계정 상태 제어 | 정상 |

### 1.3 메일 설정 (S11-16~19)

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-16** | SMTP 설정 입력·저장 | 02 영역 B·E | 메일 설정 저장 | 정상 |
| **S11-17** | 발신자 이메일·표시명 설정 | 02 영역 C·E | 발신자 정보 설정 | 정상 |
| **S11-18** | 템플릿 선택·`From`/`Subject`/`Body` 편집 | 02 영역 D·E | 메일 템플릿 커스터마이즈 | 정상 |
| **S11-19** | 테스트 메일 발송 | 02 영역 E(`[테스트 메일 발송]`) | 테스트 메일 발송 | 정상 |

### 1.4 LLM 설정 (S11-20~25)

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-20** | LLM 프로바이더 목록(OpenAI/Anthropic/Azure/로컬) | 02 영역 B | LLM 프로바이더 목록 조회 | 정상 |
| **S11-21** | 프로바이더 추가 다이얼로그 | 02 영역 A(`[+ LLM 프로바이더 추가]`) | LLM 프로바이더 추가 | 정상 |
| **S11-22** | API Key Base URL 모델명 입력 | 02 영역 C(상세) | 프로바이더 정보 입력 | 정상 |
| **S11-23** | 활성화 토글 | 02 영역 C(상세) | 프로바이더 활성화 제어 | 정상 |
| **S11-24** | 프롬프트 템플릿(시스템/예시/토큰 한도) 관리 | 02 영역 D | 프롬프트 템플릿 관리 | 정상 |
| **S11-25** | 기본값 지정(RAG용/자동 메타용) | 02 영역 E(추정) | ⚠ 화면 구현 미확정 | ⚠ 확인 필요 V-S11-3 |

### 1.5 스케줄러 관리 (S11-26~30)

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-26** | 잡 목록 테이블(이름·Cron·마지막 실행·결과·상태) | 02 영역 B | 스케줄된 잡 목록 조회 | 정상 |
| **S11-27** | 잡 상세(Cron 편집) | 02 영역 C(인라인) | 잡 설정 편집 | 정상 |
| **S11-28** | 지금 실행(수동 트리거) | 02 영역 D(`[지금 실행]`) | 잡 수동 실행 | 정상 |
| **S11-29** | 일시정지·재개 토글 | 02 영역 D(`[일시정지]`/`[재개]`) | 잡 활성화 상태 제어 | 정상 |
| **S11-30** | 실행 기록 표시(마지막 시각·결과·로그) | 02 영역 B(추가 열) | 잡 실행 기록 표시 | 정상 |

### 1.6 번역 관리 (S11-31~36)

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-31** | 언어별 번역 키·값 편집 | 02 영역 B(탭 1) | 번역 텍스트 입력·저장 | 정상 |
| **S11-32** | 카테고리별 그룹화 | 02 영역 B(탭 1) | 카테고리 트리 네비게이션 | 정상 |
| **S11-33** | 완성도 통계(%) 미번역 키 수 | 02 영역 C(탭 2) | 번역 진행 상황 통계 | 정상 |
| **S11-34** | 신규 언어 추가 | 02 영역 D(`[+ 언어 추가]`) | 지원 언어 확장 | 정상 |
| **S11-35** | CSV 수출·수입 | 02 영역 D(`[⬇ 수출]` / `[⬆ 수입]`) | CSV 파일 변환·병합 | 정상 |
| **S11-36** | 캐시 초기화(번역 즉시 반영) | 02 영역 D(`[캐시 초기화]`) | 번역 캐시 새로고침 | 정상 |

---

## 2. 비기능·품질 요건

| # | 요건 | 반영 위치 | 근거 | 상태 |
|---|---|---|---|---|
| **S11-N1** | 파괴적 동작(삭제)은 대상 이름을 확인 다이얼로그에 보여준다 | 각 화면 삭제 다이얼로그 | 00_전체 규칙 G5 | 정상 |
| **S11-N2** | 저장·삭제 중 버튼을 진행 표시로 교체하고 취소·확인을 함께 잠근다 | 각 화면 폼·다이얼로그 | — | 정상 |
| **S11-N3** | 조회 중에는 진행 표시만 보이고 폼은 그리지 않는다 | 각 화면 초기 로드 | `loading` 상태 | 정상 |
| **S11-N4** | 권한 없는 접근은 404 또는 403으로 반환한다 | 각 컨트롤러의 `@PreAuthorize` | 00_전체 5절 | 정상 |
| **S11-N5** | 화면 문구는 `t` 로 감싸서 다국어를 지원한다 | 각 컴포넌트 | 00_전체 규칙 G10 | **정정 대상 C-1** |
| **S11-N7** | API 오류는 서버 메시지를 그대로 표시한다(민감 정보 제외) | 각 화면 `Alert severity="error"` | — | 정상 |
| **S11-N8** | 에러 발생 후에도 입력값을 유지해 재시도를 편하게 한다 | 폼 상태 관리 | — | 정상 |

---

## 3. 정정 대상

동작하지만 규약과 어긋나 손볼 항목 4건이다.

| # | 항목 | 무엇이 어긋나는가 | 조치 |
|---|---|---|---|
| **C-1** | 번역 키 미지정 | UI 문구가 하드코딩 한국어다 | `t("admin.label.…", "한국어")` 로 감싸고 키를 `i18n/keys.json` 에 추가한다. 절차는 `testcasecraft-i18n-audit` |
| **C-3** | 매니저 권한 미구현 | 관리 메뉴에 MANAGER 권한을 정의했으나 실제로는 ADMIN만 접근 가능하다 | 매니저에게 일부 기능(조회·제어)을 허용할지, 아니면 ADMIN 전용으로 유지할지 확인 후 정정한다 |
| **C-4** | 번역 관리 메뉴 숨김 | `/translation-management` 라우트는 존재하지만 헤더 메뉴에 노출되지 않는다 | URL 직접 입력으로만 접근 가능한 상태가 설계인지, 메뉴에 노출할지 확인 후 정정한다 |

---

## 4. 확인 필요

화면만 보고는 판정이 갈려 확인이 필요한 항목 4건이다.

| # | 항목 | 왜 갈리는가 | 확인 방법 |
|---|---|---|---|
| **V-S11-1** | MANAGER 권한 범위 | 관리 메뉴 허용 판정가 정의되었으나 사용 안 됨 | 검사 로직 + 각 컨트롤러의 `@PreAuthorize` 검사해, MANAGER가 정말 S11 접근 불가인지 확인 |
| **V-S11-2** | 스케줄러 API 권한 | `GET /api/admin/scheduler/configs` `/configs/{taskKey}` 에 권한 검사가 없음 | 메서드 선언 확인 |
| **V-S11-3** | LLM 기본값 선택 방식 | 02 영역 E에 기본값을 어떻게 지정하는지 적히지 않았다 | 화면을 열어 라디오·토글 중 무엇으로 고르는지 확인한다 |
| **V-S11-4** | 조직 권한과 프로젝트 권한의 관계 | 조직 역할(OWNER·ADMIN·MEMBER)이 프로젝트 권한 판정에 영향을 주는가 | 조직 역할만 가진 사용자로 프로젝트 편집을 시도해 확인한다 |

---

## 5. 화면에 없는 백엔드 기능

| 기능 | 성격 |
|---|---|
| 감사 로그(감사자 조회·엔티티별 조회·통계) | 관리자가 누가 무엇을 했는지 추적 |
| 사용자 활동 로그(활동 시간·IP·기기) | 보안·이상 탐지 |
| 시스템 설정(전역 플래그·메타) | RAG·탐색 세션·통합 설정 |
| RAG 유지보수(고아 문서 정리) | 임베딩 최적화 |

---

## 6. 유지보수 인계

1.2. **확인 필요 4건(V-S11-1~4)은 코드 검사 후 이 문서의 해당 항목을 갱신한다**: 확인 결과를 행으로 추가.
3. **권한표는 세 곳에 있다**: 01 5절 02 5절 03 5절. V-S11-1이 정답이 나오면 세 표를 함께 고친다.
4. **신규 관리 화면이 추가되면**: 01 3절에 절 추가 02 1.2절 영역 표 추가 03 1절 트리 추가 04 1절요건 행 추가.
5. **메일·LLM·스케줄러·번역 관리가 변경되면**: 관련된 세 문서(01·02·03)의 해당 섹션을 함께 갱신한다.
