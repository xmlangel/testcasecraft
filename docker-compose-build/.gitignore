# JAR files copied for Docker build
app.jar
*.jar
# backup_jar() 가 재빌드 전에 남기는 타임스탬프 사본 (app.jar.20260728172804 등)
app.jar.*

# Log files
logs/

# SSL certificates (security)
ssl/*.p12
ssl/*.pfx
ssl/*.jks
ssl/*.keystore
ssl/*.crt
ssl/*.pem
ssl/*.key
ssl/*.cer

# Keep README files
!ssl/README.md

# Docker volumes
postgres_data/

data/*